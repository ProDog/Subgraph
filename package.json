{
  "name": "tryproxy_arbitrum_rinkeby",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ tryproxy_arbitrum_rinkeby",
    "create-local": "graph create --node http://localhost:8020/ tryproxy_arbitrum_rinkeby",
    "remove-local": "graph remove --node http://localhost:8020/ tryproxy_arbitrum_rinkeby",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 tryproxy_arbitrum_rinkeby"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.1",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
